{
  "@context": [
    "{{base}}/contexts/nexus/core/schema/v0.1.0",
    {
      "this": "{{base}}/schemas/nexus/core/instance/v0.1.0/shapes/"
    }
  ],
  "@type": "nxv:Schema",
  "imports": [
    "{{base}}/schemas/nexus/core/resource/v0.1.0"
  ],
  "shapes": [
    {
      "@id": "this:InstanceShape",
      "@type": "sh:NodeShape",
      "label": "Nexus instance shape",
      "comment": "A nexus instance is a REST resource (with rev, deprecated and links properties) and have distributions information to download the data it describes.",
      "targetClass": [
        "nxv:Instance"
      ],
      "targetSubjectsOf": [
        "nxv:rev",
        "nxv:deprecated",
        "nxv:links"
      ],
      "and": [
        {
          "node": "{{base}}/schemas/nexus/core/resource/v0.1.0/shapes/ResourceShape"
        },
        {
          "property": [
            {
              "path": "schema:distribution",
              "name": "Distribution",
              "description": "Details about where the data attached to this instance can be downloaded.",
              "node": "{{base}}/schemas/nexus/core/datadownload/v0.1.0/shapes/DataDownloadShape"
            }
          ]
        }
      ]
    }
  ]
}